<UserControl x:Class="Chrono.Views.BookDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chrono.Views"
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="1500">
    
    <Grid Background="White">
        <Border Style="{StaticResource CenterViewBorderStyle}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- #Title -->
                <Border Grid.Row="0">
                    <Label
                        Content="Book Details"
                        Style="{StaticResource ViewTitleLabelStyle}"/>
                </Border>
                
                <!-- #Content -->
                <Border
                    Grid.Row="1"
                    Style="{StaticResource CenterViewContentsBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="150"/>
                            <ColumnDefinition Width="Auto" MinWidth="150"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Label Grid.Column="0" Grid.Row="0" Content="Name"/>
                        <Label Grid.Column="1" Grid.Row="0" Content="{Binding CurrentBook.Name}"/>

                        <Label Grid.Column="0" Grid.Row="1" Content="Created On"/>
                        <Label Grid.Column="1" Grid.Row="1" Content="{Binding CurrentBook.CreatedDateString}"/>

                        <Label Grid.Column="0" Grid.Row="2" Content="Status"/>
                        <Label Grid.Column="1" Grid.Row="2" Content="{Binding CurrentBook.StatusString}"/>
                    </Grid>
                </Border>
                
                <!-- #Controls -->
                <Border Grid.Row="2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <StackPanel.Resources>
                            <Style TargetType="Button" BasedOn="{StaticResource StandardControlButtonStyle}"/>
                        </StackPanel.Resources>

                        <Button
                            Content="Edit"/>
                        <Button
                            Content="Archive"/>
                        <Button
                            Content="Delete"/>
                    </StackPanel>
                </Border>
            </Grid>
            
        </Border>
    </Grid>
    
</UserControl>
